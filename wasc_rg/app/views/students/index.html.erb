<h1>Listing students</h1>

<table>
  <thead>
    <tr>
      <th>Student</th>
      <th>Full name</th>
      <th>Last name</th>
      <th>First name</th>
      <th>Username</th>
      <th>Grade basis</th>
      <th>Academic lev</th>
      <th>Availability</th>
      <th>Course</th>
      <th>Program</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @students.each do |student| %>
      <tr>
        <td><%= student.student_id %></td>
        <td><%= student.full_name %></td>
        <td><%= student.last_name %></td>
        <td><%= student.first_name %></td>
        <td><%= student.username %></td>
        <td><%= student.grade_basis %></td>
        <td><%= student.academic_lev %></td>
        <td><%= student.availability %></td>
        <td><%= student.course %></td>
        <td><%= student.program.name %></td>
        <td><%= link_to 'Show', student %></td>
        <td><%= link_to 'Edit', edit_student_path(student) %></td>
        <td><%= link_to 'Destroy', student, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<!-- This code is not being displayed on page because students should be loaded
     via the course view and using the roaster created from PeopleSoft -->

		<% link_to 'New Student', new_student_path %>
