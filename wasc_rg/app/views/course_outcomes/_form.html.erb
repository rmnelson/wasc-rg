<%= form_for(@path) do |f| %>
  <% if @course_outcome.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course_outcome.errors.count, "error") %> prohibited this course_outcome from being saved:</h2>

      <ul>
      <% @course_outcome.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :course_work_id %><br>
    <%= f.collection_select :course_work_id, CourseWork.all, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :outcome_id %><br>
    <%= f.collection_select :outcome_id, Outcome.all, :id, :objective_letter %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
